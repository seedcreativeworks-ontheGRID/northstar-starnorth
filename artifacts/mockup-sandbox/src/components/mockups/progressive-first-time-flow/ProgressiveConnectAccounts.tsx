import React,{useState} from "react";
import { ArrowRight, ShieldCheck } from "lucide-react";
import { Primary, Secondary, SetupHeader, Shell } from "./_shared";
export function ProgressiveConnectAccounts(){const [choice,setChoice]=useState("northstar");const options=[["northstar","Link an existing Northstar account","Use an eligible account already associated with your business."],["institution","Add another institution","Connect an external institution using its secure sign-in."],["later","Continue without connecting now","Home will stay empty until an account is connected. You can do this later."]];return <Shell setup stage={1}><SetupHeader eyebrow="Connect accounts" title="Bring accounts into your workspace." copy="Choose what works for today. Connecting an account does not move money or change access." step="Step 2 of 4"/><div className="grid max-w-[880px] grid-cols-[1fr_255px] gap-5"><section className="rounded-lg border border-[#d6e2eb] bg-white p-6"><fieldset><legend className="text-[12px] font-bold text-[#315774]">How would you like to start?</legend><div className="mt-4 space-y-3">{options.map(([id,title,copy])=><label key={id} className={`block cursor-pointer rounded-md border p-4 ${choice===id?"border-[#6ba6d5] bg-[#f2f8fc]":"border-[#dbe5ed] hover:border-[#b9d2e5]"}`}><span className="flex gap-3"><input checked={choice===id} onChange={()=>setChoice(id)} type="radio" name="account" className="mt-0.5 accent-[#145aa3]"/><span><span className="block text-[12px] font-bold text-[#315774]">{title}</span><span className="mt-1 block text-[11px] leading-5 text-[#70899d]">{copy}</span></span></span></label>)}</div></fieldset><div className="mt-6 flex justify-between"><button className={Secondary}>Back</button><button className={`inline-flex items-center gap-2 ${Primary}`}>{choice==="later"?"Skip and continue":"Continue securely"}<ArrowRight size={14}/></button></div></section><aside className="rounded-lg border border-[#d2e0eb] bg-[#edf6fc] p-5"><ShieldCheck size={20} className="text-[#226bae]"/><h2 className="mt-4 text-[13px] font-bold text-[#315774]">Your choice stays flexible</h2><p className="mt-2 text-[11px] leading-5 text-[#668198]">Skip now to complete team setup. No balances or connected-account facts are shown during setup.</p></aside></div></Shell>}