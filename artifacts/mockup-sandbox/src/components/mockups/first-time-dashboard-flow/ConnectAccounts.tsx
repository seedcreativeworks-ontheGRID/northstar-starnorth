import React, { useState } from "react";
import { ArrowRight, Building2, Link2, ShieldCheck } from "lucide-react";
import { AppShell, Primary, Secondary, SecurityNote, StageHeader } from "./_shared";
export function ConnectAccounts() {
 const [choice,setChoice]=useState("northstar");
 const options=[["northstar","Link an existing Northstar account","Use an eligible account already associated with your business."],["institution","Add another institution","Connect an external institution using its secure sign-in."],["later","Continue without connecting now","Home will stay empty until an account is connected. You can do this later."]];
 return <AppShell stage={1}><StageHeader eyebrow="Connect accounts" title="Bring accounts into your workspace." copy="Choose what works for today. Connecting an account does not move money or change account access." step="Step 2 of 5"/>
 <div className="grid max-w-[850px] grid-cols-[1fr_270px] gap-5"><section className="rounded-lg border border-[#d6e2eb] bg-white p-6 shadow-[0_8px_20px_rgba(34,71,105,.05)]"><fieldset><legend className="text-[12px] font-bold text-[#315774]">How would you like to start?</legend><div className="mt-4 space-y-3">{options.map(([id,title,copy])=><label key={id} className={`block cursor-pointer rounded-md border p-4 ${choice===id?"border-[#6ba6d5] bg-[#f2f8fc]":"border-[#dbe5ed] hover:border-[#b9d2e5]"}`}><span className="flex gap-3"><input checked={choice===id} onChange={()=>setChoice(id)} type="radio" name="account" className="mt-0.5 accent-[#145aa3]"/><span><span className="block text-[12px] font-bold text-[#315774]">{title}</span><span className="mt-1 block text-[11px] leading-5 text-[#70899d]">{copy}</span></span></span></label>)}</div></fieldset><div className="mt-6 flex justify-between"><button className={Secondary}>Back</button><button className={`inline-flex items-center gap-2 ${Primary}`}>{choice==="later"?"Save and continue":"Continue securely"}<ArrowRight size={14}/></button></div></section><aside><div className="rounded-lg border border-[#d2e0eb] bg-[#edf6fc] p-5"><ShieldCheck className="text-[#226bae]" size={20}/><h2 className="mt-4 text-[13px] font-bold text-[#315774]">Your choice stays flexible</h2><p className="mt-2 text-[11px] leading-5 text-[#668198]">Skip for now to complete team setup. Account information will not appear on Home until a connection is made.</p></div><div className="mt-4 flex gap-2 text-[10px] text-[#7891a5]"><Link2 size={14}/><span>No balances are shown during setup.</span></div></aside></div><div className="mt-5 max-w-[850px]"><SecurityNote/></div></AppShell>;
}